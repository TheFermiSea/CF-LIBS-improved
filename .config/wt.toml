# Worktree configuration.
#
# The former post-create/post-switch hooks invoked scripts/beadhub-bootstrap.sh,
# but BeadHub was removed from this project (CLAUDE.md: commit 6b64e93) and that
# script no longer exists. No bootstrap hook is needed for native `bd` worktrees.
