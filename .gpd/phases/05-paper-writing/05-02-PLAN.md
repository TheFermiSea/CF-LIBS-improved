---
phase: 05-paper-writing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/figures/fig1_pipeline_architecture.pdf
  - paper/figures/fig2_voigt_throughput.pdf
  - paper/figures/fig3_boltzmann_speedup.pdf
  - paper/figures/fig4_anderson_convergence.pdf
  - paper/figures/fig5_faiss_latency.pdf
  - paper/figures/fig6_batch_scaling.pdf
  - paper/figures/fig7_e2e_breakdown.pdf
  - paper/scripts/generate_figures.py
interactive: false

conventions:
  units: "SI with eV for temperature, cm^-3 for densities, nm for wavelengths"
  figure_style: "JQSRT publication quality, single/double column as appropriate"
  font_size: "axis labels 10pt, tick labels 8pt, legend 8pt"
  color_scheme: "colorblind-safe (blue=#0072B2, orange=#D55E00, green=#009E73)"

estimated_execution:
  total_minutes: 45
  breakdown:
    - task: 1
      minutes: 25
      note: "Generate 6 data-driven figures from CSVs"
    - task: 2
      minutes: 20
      note: "Pipeline architecture diagram + figure integration"

contract:
  scope:
    question: "What are the 7 publication-quality figures for the JQSRT paper?"
  claims:
    - id: claim-figures-complete
      statement: "All 7 figures generated from actual benchmark data with JQSRT-appropriate styling"
      deliverables: [deliv-figure-script, deliv-figures]
      acceptance_tests: [test-figures-exist, test-data-source, test-style]
      references: [ref-exojax, ref-zaghloul2024]
  references:
    - id: ref-exojax
      kind: paper
      locator: "arXiv:2105.14782"
      role: benchmark
      why_it_matters: "JAX spectral models on GPU, throughput comparison target"
      applies_to: [claim-figures-complete]
      must_surface: true
      required_actions: [cite]
    - id: ref-zaghloul2024
      kind: paper
      locator: "arXiv:2411.00917"
      role: benchmark
      why_it_matters: "Voigt accuracy reference for figure annotations"
      applies_to: [claim-figures-complete]
      must_surface: true
      required_actions: [cite]
  deliverables:
    - id: deliv-figure-script
      kind: code
      path: "paper/scripts/generate_figures.py"
      description: "Self-contained figure generation script reading from benchmarks/figures/ CSVs"
      must_contain: ["matplotlib", "fig2", "fig3", "fig4", "fig5", "fig6", "fig7"]
    - id: deliv-figures
      kind: dataset
      path: "paper/figures/"
      description: "7 PDF figures for the manuscript"
      must_contain: ["fig1", "fig2", "fig3", "fig4", "fig5", "fig6", "fig7"]
  acceptance_tests:
    - id: test-figures-exist
      subject: claim-figures-complete
      kind: existence
      procedure: "Verify all 7 figure PDFs exist in paper/figures/"
      pass_condition: "7 PDF files present"
      evidence_required: [deliv-figures]
    - id: test-data-source
      subject: claim-figures-complete
      kind: consistency
      procedure: "Verify figure script reads from benchmarks/figures/ CSVs, not hardcoded data"
      pass_condition: "Script uses pd.read_csv or np.loadtxt to load actual benchmark data"
      evidence_required: [deliv-figure-script]
    - id: test-style
      subject: claim-figures-complete
      kind: human_review
      procedure: "Verify figures use consistent styling: colorblind-safe palette, appropriate font sizes, axis labels with units"
      pass_condition: "All figures pass visual inspection for publication quality"
      evidence_required: [deliv-figures]
  forbidden_proxies:
    - id: fp-fabricated-figure-data
      subject: claim-figures-complete
      proxy: "Figures generated from hardcoded or fabricated data instead of actual benchmark CSVs"
      reason: "Figures MUST read from benchmarks/figures/*.csv which contain real V100S data"
    - id: fp-matplotlib-defaults
      subject: claim-figures-complete
      proxy: "Figures using matplotlib default styling without publication-quality customization"
      reason: "JQSRT requires high-quality figures with proper labels, units, legends"
  uncertainty_markers:
    weakest_anchors:
      - "FAISS GPU data is null -- Figure 5 may need CPU-only data or be marked as future work"
    disconfirming_observations:
      - "Figures generated from mock/placeholder data instead of actual V100S benchmark CSVs would invalidate all visual claims"
---

<objective>
Generate all 7 publication-quality figures for the JQSRT paper from benchmark CSV data.

Purpose: Create the visual evidence supporting all performance and accuracy claims in the manuscript.
Output: 7 PDF figures in paper/figures/ and a reproducible generation script in paper/scripts/.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md

# Source data for figures
@benchmarks/figures/benchmark_summary.json
@benchmarks/figures/fig2_voigt_throughput.csv
@benchmarks/figures/fig3_boltzmann_speedup.csv
@benchmarks/figures/fig4a_anderson_iteration_counts.csv
@benchmarks/figures/fig4b_anderson_residual_trajectories.csv
@benchmarks/figures/fig5_faiss_latency.csv
@benchmarks/figures/fig6_batch_scaling.csv
@benchmarks/figures/fig7_e2e_breakdown.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate data-driven figures (Figures 2-7)</name>
  <files>paper/scripts/generate_figures.py, paper/figures/fig2_voigt_throughput.pdf, paper/figures/fig3_boltzmann_speedup.pdf, paper/figures/fig4_anderson_convergence.pdf, paper/figures/fig5_faiss_latency.pdf, paper/figures/fig6_batch_scaling.pdf, paper/figures/fig7_e2e_breakdown.pdf</files>
  <action>
    Create paper/scripts/generate_figures.py that reads benchmark CSVs and produces publication-quality PDF figures.

    **Global style setup:**
    - Use matplotlib with rcParams for JQSRT: font.family='serif', font.size=10, axes.labelsize=10, xtick.labelsize=8, ytick.labelsize=8, legend.fontsize=8
    - Colorblind-safe palette: CPU=#0072B2 (blue), GPU=#D55E00 (orange), reference=#009E73 (green)
    - Single-column width: 3.5 inches. Double-column: 7.0 inches. DPI=300 for PDF.
    - Use tight_layout() and savefig with bbox_inches='tight'

    **Figure 2: Voigt Throughput** (double-column)
    - Read benchmarks/figures/fig2_voigt_throughput.csv
    - Log-log plot: grid_size (x) vs throughput profiles/sec (y) for CPU and GPU
    - Secondary y-axis or annotation: speedup factor
    - Mark crossover point where GPU > CPU
    - Title: omit (JQSRT uses captions). Axis labels: "Grid size (points)", "Throughput (profiles/s)"

    **Figure 3: Boltzmann Speedup** (single-column)
    - Read benchmarks/figures/fig3_boltzmann_speedup.csv
    - Need to aggregate: group by element_count, take maximum speedup or plot time vs element_count
    - Two lines: CPU time, GPU time vs element count. Inset or secondary axis: speedup
    - Axis labels: "Number of elements", "Fitting time (ms)"

    **Figure 4: Anderson Convergence** (double-column, two panels)
    - Panel (a): Read fig4a_anderson_iteration_counts.csv -- iteration count vs M for different conditions
    - Panel (b): Read fig4b_anderson_residual_trajectories.csv -- residual vs iteration for Picard vs Anderson
    - Use subplot(1,2) layout. Panel labels (a), (b).

    **Figure 5: FAISS Latency** (single-column)
    - Read benchmarks/figures/fig5_faiss_latency.csv
    - Check data: if FAISS GPU data is null/empty, plot CPU-only with note
    - Axis labels: "Database size", "Query latency (ms)"

    **Figure 6: Batch Forward Scaling** (single-column)
    - Read benchmarks/figures/fig6_batch_scaling.csv
    - Log-log plot: batch_size (x) vs spectra/sec (y) for CPU and GPU
    - Annotate peak: 10,708 spectra/sec
    - Mark crossover point
    - Axis labels: "Batch size", "Throughput (spectra/s)"

    **Figure 7: E2E Pipeline Breakdown** (double-column)
    - Read benchmarks/figures/fig7_e2e_breakdown.csv
    - Grouped bar chart or stacked bar: component times for CPU vs GPU at batch=1, 10, 100, 1000
    - Components: saha/voigt/assembly/boltzmann/closure for CPU; transfer/forward/boltzmann/closure for GPU
    - Axis labels: "Batch size", "Pipeline time (ms)"

    All data MUST come from the CSV files. Do NOT hardcode any numerical values.
    Script should be runnable: `python paper/scripts/generate_figures.py` produces all 6 PDFs.
    Handle missing data gracefully (e.g., if FAISS CSV is empty, produce a placeholder figure).
  </action>
  <verify>
    1. Script runs without error: `python paper/scripts/generate_figures.py`
    2. All 6 PDFs created in paper/figures/
    3. No hardcoded data: all values come from pd.read_csv() calls
    4. Axis labels include units
    5. Colors are colorblind-safe
    6. Font sizes appropriate for JQSRT column widths
    7. Log-log scales used where appropriate (Voigt throughput, batch scaling)
  </verify>
  <done>6 data-driven figures generated from benchmark CSVs with publication-quality styling, all saved as PDF in paper/figures/</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline architecture diagram (Figure 1)</name>
  <files>paper/figures/fig1_pipeline_architecture.pdf</files>
  <action>
    Create Figure 1: CF-LIBS pipeline architecture diagram showing the data flow with GPU-accelerated components highlighted.

    Use matplotlib with patches/annotations (NOT tikz, to keep it self-contained) OR create as a separate Python script.

    **Diagram structure (left-to-right flow):**

    Box 1: "Measured Spectrum" (input)
    |
    v
    Box 2: "Preprocessing" (baseline removal, noise estimation) -- CPU only, gray
    |
    v
    Box 3: "Peak Detection + Line ID" (ALIAS method) -- CPU only, gray
    |
    v
    Box 4: "Boltzmann Fitting" -- GPU accelerated, orange highlight
    |  \
    v   v
    Box 5: "Saha-Boltzmann Solver" (Anderson acc.) -- GPU accelerated, orange
    |
    v
    Box 6: "Closure" (softmax) -- GPU accelerated, orange
    |
    v
    Box 7: "Forward Model" (batch) -- GPU accelerated, orange
    |
    v
    Box 8: "Voigt Profile Generation" -- GPU accelerated, orange (sub-component of forward)
    |
    v
    Box 9: "T, n_e, Compositions" (output)

    Also show the batch forward model path separately (for manifold generation):
    Parameters -> Batch Forward Model -> Synthetic Spectra -> FAISS Index

    Use two colors: gray for CPU-only stages, orange (#D55E00) for GPU-accelerated stages.
    Include legend: "CPU only" (gray), "GPU accelerated" (orange).
    Annotations showing the 5 optimization targets (Voigt, Boltzmann, Anderson, Softmax, Batch).

    Add to the generate_figures.py script or create paper/scripts/fig1_architecture.py.
    Output: paper/figures/fig1_pipeline_architecture.pdf
  </action>
  <verify>
    1. PDF created at paper/figures/fig1_pipeline_architecture.pdf
    2. All 5 GPU-accelerated components visually highlighted
    3. Data flow direction clear (top-to-bottom or left-to-right)
    4. Legend distinguishes CPU vs GPU stages
    5. Text readable at JQSRT double-column width (7 inches)
    6. No external dependencies (tikz, inkscape) -- pure matplotlib
  </verify>
  <done>Pipeline architecture diagram created showing CF-LIBS workflow with all 5 GPU-accelerated components highlighted, saved as PDF</done>
</task>

</tasks>

<verification>
- All 7 figure PDFs exist in paper/figures/
- Figure generation is reproducible: running the script(s) regenerates identical figures
- All data-driven figures use benchmark CSV data, not hardcoded values
- Consistent styling across all figures (colors, fonts, sizes)
- Figure labels match \ref{fig:XXX} labels used in Plan 05-01 text
</verification>

<success_criteria>
7 publication-quality PDF figures in paper/figures/, generated from a reproducible script that reads benchmark CSVs. Consistent JQSRT-appropriate styling. Pipeline architecture diagram clearly shows GPU-accelerated components. All data-driven figures use actual V100S benchmark data.
</success_criteria>

<output>
After completion, create `.gpd/phases/05-paper-writing/05-02-SUMMARY.md`
</output>
