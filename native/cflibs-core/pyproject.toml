[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "cflibs-core"
version = "0.1.0"
description = "High-performance computational core for CF-LIBS"
requires-python = ">=3.10"

[tool.maturin]
module-name = "cflibs._core"
features = ["pyo3/extension-module"]
